template:
  - sensor:
      - name: "Gianluca's Room People List"
        unique_id: gianluca_room_people_list
        state: >
          {% set people = expand('group.people_sensors') %}
          {% set in_room = people | selectattr('attributes.room_presence', 'equalto', 'gianlucas_room') | map(attribute='entity_id') | list %}
          {{ in_room | length }}
        attributes:
          people: >
            {% set people = expand('group.people_sensors') %}
            {% set in_room = people | selectattr('attributes.room_presence', 'equalto', 'gianlucas_room') | map(attribute='entity_id') | list %}
            {{ in_room }}