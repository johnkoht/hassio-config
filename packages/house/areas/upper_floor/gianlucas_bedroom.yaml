# 
# Gianluca's Bedroom Sensor
# 
template:
  - sensor:
      - name: "Gianluca's Bedroom"
        unique_id: gianlucas_bedroom
        state: "{{ states('input_boolean.gianlucas_room_occupied') }}"
        attributes:
          mode: "{{ states('input_select.gianlucas_bedroom') | default('Auto') }}"
          icon: "kohbo:kohbo-bed-single"
          permalink: "/dashboard-kohbo/rooms-gianluca-bedroom"
          temperature: >
            {% if states('sensor.gianluca_bedroom_awair_temperature') not in ['unavailable', 'unknown', ''] %}
              {{ states('sensor.gianluca_bedroom_awair_temperature') | float }}
            {% else %}
              ""
            {% endif %}
          humidity: >
            {% if states('sensor.gianluca_bedroom_awair_humidity') not in ['unavailable', 'unknown', ''] %}
              {{ states('sensor.gianluca_bedroom_awair_humidity') | float }}
            {% else %}
              ""
            {% endif %}
          aqi: >
            {% if states('sensor.gianluca_bedroom_air_quality') not in ['unavailable', 'unknown', ''] %}
              {{ states('sensor.gianluca_bedroom_air_quality') }}
            {% else %}
              ""
            {% endif %}
          last_changed: >
            {% if states.input_boolean.gianlucas_room_occupied.last_changed %}
              {{ states.input_boolean.gianlucas_room_occupied.last_changed.isoformat() }}
            {% else %}
              {{ now().isoformat() }}
            {% endif %}
          occupied_duration_seconds: >
            {% if is_state('input_boolean.gianlucas_room_occupied', 'on') and states.input_boolean.gianlucas_room_occupied.last_changed %}
              {{ (as_timestamp(now()) - as_timestamp(states.input_boolean.gianlucas_room_occupied.last_changed)) | int }}
            {% else %}
              0
            {% endif %}
          occupied_duration_minutes: >
            {% if is_state('input_boolean.gianlucas_room_occupied', 'on') and states.input_boolean.gianlucas_room_occupied.last_changed %}
              {{ ((as_timestamp(now()) - as_timestamp(states.input_boolean.gianlucas_room_occupied.last_changed)) / 60) | int }}
            {% else %}
              0
            {% endif %}
          people: "{{ state_attr('sensor.gianlucas_room_people_list', 'people') | default([]) }}"
          people_count: "{{ states('sensor.gianlucas_room_people_list') | int | default(0) }}"
