template:
  - binary_sensor:
      - name: "John Probably in Bed"
        unique_id: john_probably_in_bed
        default_entity_id: binary_sensor.john_probably_in_bed
        state: >-
          {{ state_attr("binary_sensor.john_in_bed", "probability") > 0.5 }}
