template:
  - binary_sensor:
      - name: John probably in a meeting
        unique_id: john_probably_in_meeting
        default_entity_id: binary_sensor.john_probably_in_meeting
        state: >-
          {{ state_attr("binary_sensor.john_do_not_disturb", "probability") > 0.65 }}
