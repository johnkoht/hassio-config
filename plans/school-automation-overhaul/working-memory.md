# Working Memory — school-automation-overhaul

Cross-task knowledge. Every developer reads this before starting and updates it after completing.

## Seeded context — read before any task

**Repo shape**: the repo root IS the Home Assistant config dir. There is no `homeassistant/` subdirectory. Packages live at `packages/`.

**Hard constraints, verified this session — violating any of these is a build failure, not a style nit:**

1. **Never `initial:`** on an `input_datetime` / `input_number` / `input_boolean` that a human is meant to edit. It disables state restoration (`# Don't restore if we got an initial value.`), so every dashboard edit silently reverts on the next HA restart. Real values go in header comments.
2. **`hours: /1`, never `hours: 1`.** `hours: 1` in a `time_pattern` matches hour == 1, i.e. fires once daily at 01:00. Four existing files have this bug.
3. **Every trigger-based template needs `event_template_reloaded`** alongside `homeassistant` start — HA-start does not fire on reload, so a reloaded sensor sits at `unknown` until the next tick.
4. **Filenames must be globally unique.** `!include_dir_named` keys packages by basename across the whole tree; duplicates collide silently.
5. **`.yml` is never loaded** — always `.yaml`.
6. **Modern syntax only**: `triggers:` / `conditions:` / `actions:`, `trigger:` / `action:` keys, `target:` blocks. No `platform:`, no `service:`, no root-level `entity_id:`.
7. **Timestamp sensors must be timezone-aware**: `today_at('00:00') + timedelta(seconds=state_attr('input_datetime.x','timestamp'))`. `as_datetime()` on a concatenated string yields naive, which makes `SensorEntity.state` raise under `device_class: timestamp`.
8. **Never restart HA.** Reload specific domains. John deploys himself.

**Track A is additive.** The old school system stays live so it can be shadow-run against the new one. Do not delete anything still referenced by `packages/reminders/morning_update.yaml` or by a surviving automation — those deletions are Track C.

**No `!secret` in Track A.** Calendar entity IDs go inline with a `# TODO(track-b)` marker. The keys don't exist locally or on the host yet, so `!secret` would fail `ha core check`.

## ⚠️ Validating config in this worktree — read before task-16

`secrets.yaml` is gitignored, so **this worktree does not have one** (verified). Every `!secret` reference in the repo — 40 keys — would fail `check_config` here. Task-1 removed the automatic `cp secrets.fake.yaml secrets.yaml` from `deploy.sh`, so that no longer happens for you.

Verified: `secrets.fake.yaml` contains 51 keys and covers **all 40** referenced keys, so it is sufficient for validation.

Procedure for task-16, in the worktree only:

```bash
cp secrets.fake.yaml secrets.yaml     # temporary, gitignored
./deploy.sh --check                   # or the docker check_config directly
rm -f secrets.yaml                    # ALWAYS clean up
```

**Never run this in the main repo** at `/Users/johnkoht/code/hassio-config`. A real `secrets.yaml` lives there, and copying the fake over it destroys it permanently — that is the exact data-loss bug task-1 just removed. The copy is safe here *only* because this worktree has no real file.

Track A adds **no new `!secret` keys**, so no update to `secrets.fake.yaml` is needed.

## Discovered Patterns
*(Add: [Task N] pattern-name: description at file:line)*

- [seed] Trigger-based template + `calendar.get_events` + `response_variable`: existing reference at `packages/school/primary_school/primary_school_closed.yaml` (note: copy the shape, not the `hours: 1` bug or the `'District Closed'` clause).
- [seed] TTS entry point is `script.voice_announcement` (`packages/announcements/voice_announcement.yaml`); push is `script.general_notification` (`packages/general_notifications.yaml`, devices `all` | `jk` | `cfalb`). Gating for speech (`speech_notifications`, time window, bedtime) is inside the script — do not duplicate it in callers.
- [seed] Sensor-with-payload convention: `state: "{{ now().isoformat() }}"` plus attributes, to stay under the 255-char state limit. Reference: `primary_school_lunch_menu.yaml`.
- [Task 6] Classify-once-then-derive pattern: one namespace-based `{% for %}` loop builds a native list of dicts (`classified`) in a single `variables:` entry; every downstream value (`closed_today`, `tier1_today`, …) is a one-line `selectattr`/`map` expression over that list in later `variables:` entries, not a second copy of the loop. Mirrors the `display`/`items`/`entrees` chain in `primary_school_lunch_menu.yaml:40-74`, generalized to 6 derived outputs instead of 2. Reference: `packages/school/district_calendar_sensors.yaml`.
- [Task 7,8] Timestamp-sensor pair pattern (`state:` + `availability:` templates repeating the same input-selection logic rather than sharing it via an entity-level `variables:` key): every `device_class: timestamp` sensor's `state:` and `availability:` templates independently re-derive which `input_datetime` entity applies (e.g. late-start-vs-normal in the three D34 files, weekday-branch in the parochial file) rather than computing it once and sharing across both templates. Chose repetition over an entity-level `variables:` block because no existing file in `packages/` uses that key inside a state-based (non-trigger) `template:` sensor/binary_sensor list item — introducing an unverified HA schema feature here felt riskier than a few duplicated lines. Verified safe by rendering every `state:`/`availability:` template through a standalone Jinja2 `Environment` with both nominal inputs and the shipped/all-unset state — no exceptions, no stray whitespace in the `isoformat()` output, `availability` correctly flips to `False` (not raise) when inputs are unset. Reference: `packages/school/schools/primary_school_schedule.yaml` (`primary_school_start_time_today`).
- [Task 7,8] `in_session_tomorrow` needs a same-shape date-bounds check as `binary_sensor.school_year_active` (`school_year.yaml`) but there's no "tomorrow" variant of that sensor to read — inlined the same `states('input_datetime.school_year_first_day') not in [...]` guard plus a lexicographic date-string comparison against `(now() + timedelta(days=1)).strftime('%Y-%m-%d')`, rather than adding a new shared sensor. Reference: `packages/school/schools/primary_school_schedule.yaml` (`primary_school_in_session_tomorrow`).
- [Task 15] A plain `type: entities` row renders a working native picker for `input_datetime`/`input_number` regardless of the entity registry's `editable: false` (that flag only gates the Settings > Helpers UI, not the entities-card row widget). Wrapped each editable group in the same `stack-in-card` + `kohbo_vertical_stack_bg` + `kohbo_entities_container` + `entity_no_icon` card_mod chain already used for `input_datetime.alarm_john_away_arm_time` in `dashboards/kohbo/security/pages/alarm_panel.yaml` — did not invent a new pattern. Read-only booleans reuse `kohbo_boolean_entity_layout.yaml` even when applied to a `binary_sensor` (no toggle switch exists on that domain, so the nested `ha-entity-toggle` selectors in that card_mod are simply inert — harmless, not worth a separate read-only variant). Reference: `dashboards/kohbo/school/school.yaml`.

## Active Gotchas
*(Add: [Task N] issue the next developer must know about)*

- [seed] `calendar.ninos_school` and `calendar.nino_school_reminder` **do not exist** on the live instance. Never reference them.
- [seed] Real calendar entities: `calendar.district_34_events`, `calendar.lyon_school`, `calendar.gianluca_school`, `calendar.olph_school` (currently empty — pointed at the wrong Blackbaud feed, fixed in Track B).
- [seed] `calendar.get_events` on a target resolving to zero entities **raises** and aborts the whole action sequence. Hence one template block per calendar plus `continue_on_error: true`.
- [seed] OLPH publishes grade-scoped closures (`Grade 2 - No School`) and multi-day all-day events (`NO SCHOOL - Thanksgiving`, 11-day `Easter Vacation`). Both break naive matching.
- [seed] `states.<domain>.<object>.state` raises `UndefinedError` on a missing entity and kills the entire template render. Use `is_state()`.
- [Task 14] `packages/reminders/morning_update.yaml` has **two independent school-data paths into the same ChatGPT prompt**, and only one of them is filtered:
  1. The tier-classified sensors (`sensor.primary_school_events_today`'s `actionable`/`informational` attributes, `binary_sensor.primary_school_closed_today`, `binary_sensor.nino_school_day`, `binary_sensor.gianluca_school_day`) — these go through the Task 6 classification pipeline (`primary_school_calendar_sensors.yaml`), so OLPH/Lyon noise (athletics practices, board meetings, etc.) is already stripped before it reaches this file.
  2. The raw `calendar.get_events` aggregation feeding `sensor.calendar_events_today` (~line 241-263 of this file) — this is a flat, untiered dump of whatever's in its `target: entity_id:` list, straight into the prompt with no filtering at all.
  This distinction is not obvious from reading either block in isolation. It's the entire reason the comment above the aggregation's target list exists: adding `calendar.olph_school` there (710 events, 523 grade 5-8 athletics) would dump all of it into the daily prompt with no tier gate, unlike the sensor path. `calendar.gianluca_school` is safe in that same list only because its feed is low-volume (~1 event/day) — not because the aggregation is filtered.
- [Task 6] **YAML folded scalars (`>-`) silently break HA's native-type `variables:` rendering — and the failure mode is silent, not an error.** A folded block scalar collapses a newline between two same-indentation lines into a single space. If your `{% for %}...{% endfor %}` loop and the trailing `{{ ns.out }}` sit at the same indentation, that fold inserts a literal space between `{% endfor %}` and `{{ ns.out }}`. Jinja's `trim_blocks`/`lstrip_blocks` only strip *newlines* adjacent to `{% %}` tags, not arbitrary spaces, so that space becomes a second text node. Home Assistant's `NativeEnvironment` only preserves native Python type (list/dict/bool) when the template's rendered output is exactly one node — two nodes falls back to string concatenation, so `ns.out` (a real list) silently becomes the *string* `"[{'summary': ...}, ...]"`. Chaining `| selectattr(...)` on that string doesn't raise (Jinja's `getattr` on a string character returns `Undefined`, which is falsy) — it just silently returns an empty list every time. Caught only by rendering the actual folded-scalar string through a standalone `jinja2.nativetypes.NativeEnvironment` with `trim_blocks=True, lstrip_blocks=True` outside HA (no live instance in this worktree) and checking `type(result)`, not by `ha core check`, which only validates YAML/schema and would have passed either way. **Fix**: use explicit `{%-`/`-%}` whitespace control on every tag in a namespace-accumulator loop feeding a `variables:` entry, so the only non-whitespace-controlled output is the final `{{ expr }}`. Reference: `packages/school/district_calendar_sensors.yaml` (`classified` variable). Open question for the next developer touching a similar pattern: `primary_school_lunch_menu.yaml`'s `entrees` variable (lines 51-60) uses the same loop-then-`{{ ns.out }}` shape *without* dash-trim, and its output is fed straight back into a plain string interpolation (`entrees: "{{ entrees if ok else ... }}"`) rather than chained through `selectattr`/`map` — so if it has the same latent bug, nothing downstream would currently expose it. Not confirmed either way; out of scope for Track A.

## Track C follow-ups (deletion phase, after the shadow-run week)

- **Rename `unique_id: primary_school_closed_tomorrow_v2`** → `primary_school_closed_tomorrow` in `packages/school/primary_school_calendar_sensors.yaml`. The `_v2` suffix exists only to avoid colliding with the still-live `primary_school_closed_tomorrow.yaml`, which Track C deletes. Renaming requires purging the old entity-registry row first, or HA will `_2`-suffix the new one.
- **Delete `packages/school/district/district_late_start_wednesday_boolean.yaml`** together with its four consumers (`primary_school_day_on.yaml:47`, `primary_school_day_off.yaml:34,39`, `school_day_reminder.yaml:39`). Withheld from Track A because `school_day_reminder.yaml:39` reads it as `states.input_boolean.….state`, which raises `UndefinedError` on a missing entity.
- Verified 2026-08-25 against 849 live calendar events: the tier classifier scores 14/14 on grade-guard cases and filters 545 of OLPH's 710 events. Re-run that check if the tier keyword lists are ever edited.

## Shared Utilities Created
*(Add: [Task N] functionName() in path/to/file)*

## Context Corrections
*(Add: [Task N] MISSING_CONTEXT: what was missing and where to find it)*

- [Task 4,5] NOTHING_NOVEL — school_year.yaml, school_overrides.yaml, and school_overrides_reset.yaml followed existing patterns exactly: `template: - binary_sensor:` with `availability:` guard (copied shape from `packages/reminders/birthdays/nino/nino_days_to_birthday.yaml`), no-`initial:` input_datetime/input_boolean (seeded constraint #1), and an ungated nightly reset citing `nino_countdown_to_birthday_turn_off.yaml` as the latch-bug precedent (seeded constraint from task instructions). No new entity names invented beyond what the task specified. Basenames confirmed globally unique via `find . -name "<basename>"`.
- [Task 6] BLOCKER_RESOLVED — the classification pipeline (day-overlap, grade-scope guard, tier ordering, dedup) is exactly one `{% for %}` loop's worth of logic, but writing it as 6 independent copies (one per output: closed_today, closed_tomorrow, tier1_today, tier1_tomorrow, tier2_today, tier2_tomorrow) would have 6x'd both the file length and the chance of the six copies drifting out of sync. Resolved by computing one native list-of-dicts (`classified`) via namespace-accumulator, then deriving every output as a one-line `selectattr`/`map` expression over it in later `variables:` entries — see the new Discovered Patterns entry above. That in turn only works if `classified` actually renders as a native list, which required the `{%-`/`-%}` whitespace-control fix documented under Active Gotchas. Also NEW_PATTERN: split a single calendar-fetching file into N independent `calendar.get_events` calls (parochial file: 2 calls, 2 response variables) within one `template:` block when N calendars share one kid/scope but must not take each other down — distinct from the file-per-calendar split at the task level, needed here because `calendar.olph_school` and `calendar.gianluca_school` both belong to the parochial scope but calendar.olph_school is known-broken until Track B.
- [Task 7,8] MISSING_CONTEXT / OTHER — `prd.md`'s task-8 acceptance criterion for `binary_sensor.parochial_school_in_session_today` reads "requires a `Gianluca School` event present on the schedule calendar AND no `no school` marker AND the override off." The direct task-8 work order given to this developer (superseding that PRD text) specified instead: the parochial closure sensor off AND the override off AND today is a weekday, noting explicitly that whether Gianluca actually has school is already confirmed by his calendar via `parochial_school_calendar_sensors.yaml`'s classification pipeline. Implemented the work-order version — no shipped sensor computes "a specific event is present" (task-6's pipeline only emits `closed_today`/`tier1`/`tier2`, not an event-presence flag), so the literal PRD wording isn't buildable against what actually shipped without inventing new unverified logic. Flagged in `prd.json` task-8 under `deviatedFromAcceptanceCriteria` rather than silently complying with either text. Next developer/reviewer: confirm which of the two definitions is intended before Track C locks this in.
- [Task 9] OTHER — `prd.json`'s task-8 `deviatedFromAcceptanceCriteria` text (above) is now stale relative to the shipped file. `packages/school/schools/parochial_school_schedule.yaml`, as it exists on disk today, computes `binary_sensor.parochial_school_in_session_today` as `is_state('binary_sensor.parochial_school_has_event_today', 'on') and not is_state('binary_sensor.parochial_school_closed_today', 'on') and is_state('input_boolean.school_day_override_off', 'off')` — i.e. it DOES key off calendar event presence (`parochial_school_has_event_today`, sourced from `calendar.gianluca_school` in `parochial_school_calendar_sensors.yaml`), closer to the original PRD wording than the "not closed AND override AND weekday" formula the deviation note describes. The file's own header comment (verified 2026-08-26) explains why: OLPH's closure feed alone silently fails open through Christmas Eve and both breaks, so `has_event_today` became the load-bearing signal after task-7/8 shipped. Not a task-9 concern to fix — task-9 only needed to mirror whatever `binary_sensor.parochial_school_in_session_today` says, and it does — but the `prd.json` task-8 deviation note should be refreshed before Track C review so it doesn't mislead a reviewer diffing json against the file.
- [Task 9] NOTHING_NOVEL — `nino_school.yaml` and `gianluca_school.yaml` followed task-7/8's established shape exactly: no-`initial:` input_number (mirrors the no-`initial:` input_datetime/input_boolean convention), `binary_sensor.<kid>_school_day` mirrors (does not re-derive) its band's `_in_session_today` sensor, timestamp sensors pair a `state:`/`availability:` template the same way the band files do. One addition beyond the band files' pattern: `as_datetime(states(...))` on a sibling *sensor's* rendered ISO string can itself raise (HA's `as_datetime` filter has no default arg in the task-specified call form, and a source sensor's `states()` value flips to the literal string `"unavailable"` when *its* availability is false) — the band files never hit this because they read `state_attr(input_datetime, 'timestamp')` (a numeric attribute, safely defaulted via `| int(0)`), not another sensor's rendered state string. Guarded with a `{%- set base = as_datetime(src) if src not in [...] else now() -%}` fallback in both `state:` templates that read a sibling sensor (Nino's departure/pickup, Gianluca's pickup); Gianluca's departure reads a raw `input_datetime` so it doesn't need the guard and uses the band files' exact `| int(0)` construction unmodified. Verified necessary (not overcautious) by rendering the all-unset shipped-state scenario through a standalone Jinja2 `Environment`: without the guard, `as_datetime('unavailable')` raises `ValueError` and would error the entity out entirely, exactly like the naive-datetime failure mode constraint #7 warns about — just reached via a different path (chained sensor-of-sensor unavailability) than the one constraint #7 originally documented (naive string concatenation).
- [Task 7,8] BLOCKER_RESOLVED — `in_session_today`'s override-boolean check (`input_boolean.school_day_override_off`) doesn't have an obvious `_tomorrow` analogue: the override is a same-day manual control that `school_overrides_reset.yaml` unconditionally clears at 23:58 every night, so whatever it reads *today* is stale by the time "tomorrow" actually starts. Decision: still read it in `in_session_tomorrow`, purely for formula symmetry with `in_session_today`, on the reasoning that its practical effect is negligible (it's back to off every night regardless) and a symmetric formula is easier to audit than an asymmetric one. Documented inline in each schedule file's header comment. Not tested against a live instance — flag if this proves wrong in the Track B shadow run.
- [Task 10-13] MISSING_CONTEXT — `prd.md`'s task-12 section names the two new pickup files `nino_pickup_reminder.yaml` / `gianluca_pickup_reminder.yaml`. The direct task-12 work order given to this developer specified `_v2` suffixes (`nino_pickup_reminder_v2.yaml` / `gianluca_pickup_reminder_v2.yaml`, ids to match) instead, superseding that PRD text, so the new automation `id:` values don't collide with the still-live originals' ids (`nino_school_pickup_reminder` / `gianluca_school_pickup_reminder`) during the shadow-run week — `prd.json`'s own task-12 acceptance criteria only require basenames not collide, not a specific name, so both texts are satisfiable simultaneously. `# TODO(track-c): rename once the original is deleted` comments are in both files. Flagged in `prd.json` task-12 under `resolutionNote`.
- [Task 14] MISSING_CONTEXT / OTHER — the direct task-14 work order's required comment text for the `calendar_events_today` target list originally read "Do not add `calendar.olph_school` or `calendar.gianluca_school` here" — but `calendar.gianluca_school` was already in that list (untouched, per the same work order's instruction to leave the other calendars as-is), so the literal text would have shipped a comment warning against an entity three lines below the warning. Flagged before committing rather than silently complying; the task's author corrected the wording mid-task to name only the real risk (`calendar.olph_school`) and note `calendar.gianluca_school`'s low volume (~1 event/day) as the reason it's fine. `prd.json` task-14's third acceptance criterion still has the old (incorrect) wording — flagged via `resolutionNote` there for prd.md correction before Track C review.
- [Task 10-13] NOTHING_NOVEL — all seven reminder files followed established patterns exactly: no `initial:` on the new `input_datetime.school_breakfast_reminder_time` (constraint #1), modern syntax throughout, `is_state()` never `states.x.y.state` (seeded gotcha), condition ladder ordering (house → kid/feature). The two departure files are the only structurally new pattern: a commented-out `script.voice_announcement` call left in place (not deleted) so enabling TTS later is a one-line uncomment, not a rewrite — mirrors how `school_overrides.yaml`'s inputs are meant to be dashboard-edited rather than code-edited. Gianluca's new pickup reminder (`gianluca_pickup_reminder_v2.yaml`) drops the label-substitution message logic the old calendar-triggered original used (`trigger.calendar_event.summary`) since the new time-based trigger has no calendar event in its context — message falls back to Nino's generic "school" phrasing instead of inventing a new source for the label.
- [Task 15] MISSING_CONTEXT — none of the tier-classification / closure / lunch-menu template sensors declare `default_entity_id:`; their `entity_id` is derived by HA slugifying `name:`, and every one of them (verified by grep before committing, 49/49) happens to slugify to exactly the `unique_id:` string already documented in each file's header comment (e.g. `name: Primary School Events Today` → `sensor.primary_school_events_today`). Also confirmed: `intermediate_school_schedule.yaml` and `middle_school_schedule.yaml` have no per-band `_closed_today` sensor of their own — both read `binary_sensor.district_closed_today`/`_tomorrow` directly (see each file's header comment), unlike primary which has its own `calendar.lyon_school` feed. The dashboard's Calendar section reflects this: Intermediate/Middle share one "District" card rather than getting two more per-band closure cards.

## Decision Ledger
*(Builder rulings that supersede or refine a documented rule.)*

- [2026-08-25] Bell schedules are runtime-editable `input_datetime` helpers, not YAML constants — enables dashboard and future-agent edits without a deploy. Requires the no-`initial:` rule above. — docs affected: none-yet — status: open
- [2026-08-25] Schools are named by **grade band** (`primary`/`intermediate`/`middle`), not by building. Glenbrook South is out of scope (District 225). — docs affected: none-yet — status: open
- [2026-08-25] Gianluca's entities are `parochial_school_*`; "Junior Kindergarten" is a display label only, since JK is a grade he ages out of. — docs affected: none-yet — status: open
- [2026-08-25] `secrets.yaml` is human-managed; no script may write to or delete it. `deploy.sh` no longer copies `secrets.fake.yaml` over it or deletes it — `check_config` now validates against whatever real `secrets.yaml` is already on disk. **Recommendation: retain `secrets.fake.yaml`.** It's no longer consumed automatically by `deploy.sh`, but it still has two uses: (1) a template of the required `!secret` keys for bootstrapping a fresh clone/worktree — this exact worktree has no `secrets.yaml` at all (gitignored, per-checkout), so a developer running `--check` here needs to manually `cp secrets.fake.yaml secrets.yaml` first or `check_config` will fail loud (acceptable — no longer silent); (2) a documented reference of the secrets schema independent of the untracked real file. Retiring it would remove that bootstrap path with no replacement. Not deleted in this task per instruction — this is a recommendation only. — docs affected: `deploy.sh` — status: folded
- [2026-08-26] `binary_sensor.parochial_school_in_session_today` implemented as (parochial closure sensor off AND override off AND weekday), per the direct task-8 work order, NOT per `prd.md`'s literal "requires a Gianluca School event present" wording — see Context Corrections above. Reviewer should confirm before Track C. — docs affected: `prd.md` (task-8 acceptance criterion is stale/ambiguous relative to what task-6 actually shipped) — status: open
- [2026-08-26] `in_session_tomorrow` (all three D34 files) reads today's `input_boolean.school_day_override_off` for formula symmetry with `in_session_today`, even though the override is conceptually same-day and gets unconditionally cleared nightly at 23:58 — see Context Corrections above. — docs affected: none-yet — status: open
- [2026-08-26] The `calendar_events_today` aggregation comment in `morning_update.yaml` warns only against `calendar.olph_school` (710 events, 523 grade 5-8 athletics), not `calendar.gianluca_school` — the latter stays in the target list untouched, at low volume (~1 event/day). This is deliberately narrower than task-14's original acceptance-criteria wording in `prd.json`, which named both calendars; that wording was wrong (it would warn against an entity already present in the list it applies to) and was corrected during task-14. — docs affected: `prd.json` task-14 (resolutionNote added), `prd.md` (acceptance criterion text should be corrected before Track C review) — status: open
